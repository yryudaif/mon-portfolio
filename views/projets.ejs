<%- include('partials/header') %>

<h2 class="text-3xl font-semibold mb-8 text-center">Mes projets</h2>

<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
  <% if (projets.length === 0) { %>
    <p>Aucun projet pour l’instant.</p>
  <% } %>

  <% projets.forEach(project => { %>
    <div class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition">
      <h3 class="text-xl font-bold mb-2"><%= project.titre %></h3>
      <p class="text-gray-700 mb-4"><%= project.description %></p>

      <% if (project.lien) { %>
        <a href="<%= project.lien %>" target="_blank" class="inline-block text-blue-600 hover:underline mb-2">🔗 Voir le projet</a>
      <% } %>

      <form action="/projets/<%= project.id %>/supprimer" method="POST" onsubmit="return confirm('Supprimer ce projet ?');">
        <button type="submit" class="text-sm text-red-500 hover:underline mt-2">🗑 Supprimer</button>
      </form>
    </div>
  <% }) %>
</div>

<%- include('partials/footer') %>
